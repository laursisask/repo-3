apiVersion: v1
kind: Service
metadata:
  name: {{ include "cog-ia-model.fullname" . }}
  labels:
    {{- include "cog-ia-model.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cog-ia-model.selectorLabels" . | nindent 4 }}
